# 05 — Deploy once and complete Production Verification

**What to build:** Deploy the reviewed recovery revision once to the sole production environment, observe live recovery without automated remediation, validate the approved five-region feed and authorization boundary, and prove the deployed presentation with one controlled non-pinging test message.

**Blocked by:** 04 — Verify the integrated Recovery Deployment candidate.

**Status:** claimed

- [ ] Immediately before mutation, re-query service state, Runtime Health Snapshot evidence, regional progress, due backlog, ESI limiter, resolver readiness, prepared deliveries, unresolved permanent deliveries, and new errors.
- [ ] Validate a fresh production backup, restore listing, migration ledger, protected migration checksum, and quiet production Compose configuration using the documented procedures.
- [ ] Deploy the exact reviewed revision once to the existing production Compose stack without creating a staging, canary, shadow, blue/green, partial-fleet, or traffic-splitting environment.
- [ ] Automatic detection can halt subsequent rollout work and publish evidence but cannot stop or restart services, change images, restore data, reverse migrations, mutate subscriptions, queue historical repairs, or perform any other remediation.
- [ ] If a regression appears, preserve the deployed state and evidence, report it, and wait for explicit operator direction.
- [ ] The Rollout Health Gate observes one complete post-deployment regional sweep within 30 minutes.
- [ ] The due backlog is smaller after that sweep, and measured terminal-recovery throughput projects clearance of the current backlog within 24 hours.
- [ ] Corrected ESI progress is current; bot, PostgreSQL, watchdog, and Structure Resolver are ready; there are no new prepared or unresolved permanent deliveries.
- [ ] No new authentication-boundary, Discord, duplicate-post, unexpected-mention, panic, database, sustained rate-limit, or unresolved collection failure blocks progression.
- [ ] Production inspection confirms the designated Contract Subscription contains Metropolis, Heimatar, The Forge, Etherium Reach, and Perrigen Falls; Sale Confirmed and Purchase Confirmed retain post-and-ping, and no other lifecycle action gains ping eligibility. Correct only detected drift.
- [ ] Production diagnostics prove public ESI remains unauthenticated and player-structure requests retain the scoped bearer token.
- [ ] Send exactly one controlled non-pinging Production Verification message to testing channel `1115807643748012072`.
- [ ] Verify the Production Verification message has correct Issuer presentation, strongest supported human-readable location, current presentation revision, no raw identifier labels, no empty placeholders, no allowed mentions, and no replacement post.
- [ ] Record deployment revision, backup, migration state, service state, sweep duration, backlog start/end, measured drain rate, limiter state, subscription state, Production Verification result, and any detected issue for handoff.
- [ ] Historical Enrichment Repair remains untouched throughout this ticket.

## Comments

### 2026-08-25 — single deployment evidence; Rollout Health Gate halted

- **Claim:** Ticket claimed before production work. No source, migration, subscription, historical-repair, rollback, restore, or remediation mutation was made by this ticket.
- **Pre-deployment candidate:** `bf29e773cb72adb35136b73f137d9bdc3952cf76`. A mode-`0700`, tracked-files-only archive was materialized at `/tmp/hazardous-killbot-ticket05-bf29e773`; its tree exactly matched `git archive` and had no `.git` or `.env`. The live checkout's `Dockerfile`, `docker-compose.yaml`, Cargo files, `src/`, and `migrations/` matched that candidate; unrelated dirty/untracked files, including the protected unstaged `ON CONFLICT` test hunk, were excluded. Candidate Compose validation passed with `docker compose --env-file .env -f docker-compose.yaml -f /tmp/hazardous-killbot-ticket05-bf29e773/compose.override.yaml config --quiet`.
- **Backup:** created at `2026-08-25T06:25:16Z`: `/Users/sean/hazardous-killbot/backups/ticket05-predeploy-20260825T062516Z.dump`, custom archive, mode `0600`, 78,276,838 bytes, SHA-256 `373c30ec6b5147df7ff0100a9fd7d994351e73ce43a8b30f4c992b7ee867c55c`. `pg_restore --list` completed with 183 entries.
- **Migrations:** all 26 `_sqlx_migrations` rows were successful; every stored SHA-384 checksum matched its candidate-equivalent migration file. PostgreSQL was healthy and unchanged during deployment.
- **Pre-mutation snapshot (`2026-08-25T06:26:56Z`):** original `discordbot` host PID `14848` was the only bot process; bot, PostgreSQL, and watchdog were running with restart count `0` and `OOMKilled=false`. Snapshot was critical only on the old deployment's contract-progress, ESI-progress, and backlog checks; 114 regions had completed observations (oldest `05:03:41Z`, newest `06:24:58Z`), due backlog was 38,557, expired limiter pause was `2026-08-21T18:56:00Z`, pacing was inactive, prepared and unresolved-permanent deliveries were zero, and no lock waits/ungranted locks existed. Resolver persisted `degraded`, but the separately authorized pre-deployment diagnostic had proved revision `1` refresh HTTP 200 and one scoped retained-structure lookup (`1022167642188`) HTTP 200 without refresh-token rotation.
- **Subscription/auth boundary:** the designated `supercap-turnur-kurniainen-8ly` subscription retained regions `[10000042,10000030,10000002,10000027,10000066]` (Metropolis, Heimatar, The Forge, Etherium Reach, Perrigen Falls); Listed/Expired/Closed were `post`, and only Sale Confirmed/Purchase Confirmed were `post_and_ping`. No drift was corrected. The production cache had zero authenticated-contract resource rows; only the scoped resolver credential variables were present, and no credential value was printed.
- **Deployment (one Compose operation):** started `2026-08-25T06:27:28Z` with `docker compose --env-file .env -f docker-compose.yaml -f /tmp/hazardous-killbot-ticket05-bf29e773/compose.override.yaml up --build -d`. It built image `sha256:90d18f11ed8c11aa545702b28c2d70067fd5cc340c6d88e9eb7dc9c94af3e856`. Compose recreated only `discordbot` (created `06:30:03Z`, host PID `38383`) and `health-watchdog` (created `06:30:03Z`); PostgreSQL remained its original container. All three were running with restart count `0` and `OOMKilled=false`. Bot logs showed Discord connected, R2Z2 resumed its persisted checkpoint, and terminal recovery began; no rollback, restart, or follow-up deployment occurred.
- **Post-deployment recovery evidence:** all 114 regions completed one new sweep: first completed `2026-08-25T06:30:15Z`, last `2026-08-25T06:31:19Z` (64 seconds, within 30 minutes). At `06:32:14Z`, Runtime Health Snapshot reported `contract_progress=healthy` (114 current regions) and corrected `esi_progress=healthy` (55 seconds old); heartbeat, R2Z2, feed validation, prepared delivery, permanent delivery, and limiter were healthy/inactive. Manifest collection failures cleared; resolution-probe failures were 32,328. Two isolated startup SQL slow-statement warnings were observed (about 1.2 seconds each), not a lock wait or sustained database outage.
- **Blocking gate result:** **failed; do not proceed.** At `06:32:14Z`, due backlog was **38,584**, versus **38,557** immediately before deployment (net **+27**; 38,559 Awaiting Resolution cases), so it was not smaller and no valid <=24-hour drain projection could be made. `structure_resolver_runtime` also remained `degraded` (`structure resolver authorization or upstream failure`, last update `06:29:04Z`) rather than becoming `ready`. The deployed state was preserved without manual health/database alteration or other remediation.
- **Production Verification:** not sent. No Discord test message, replacement post, historical-enrichment action, or post-failure authenticated diagnostic was performed.
- **Prior accidental-process incident:** an investigation intended to be read-only inadvertently invoked `./killbot-rust --help` at `2026-08-24T20:07:14Z`, launching a secondary Discord-connected `killbot-rust` exec process group separately from the original PID 1 bot. The explicitly authorized targeted SIGTERM later removed that secondary group. It exited; later `docker top` confirmed only the original bot remained, with no container restart/OOM. Delivery audit remained at zero prepared, zero unresolved permanent, and zero duplicate contract nonces. This deployment later replaced the original bot only through the one authorized Compose operation above.

**Handoff:** leave Ticket 05 claimed and pending Sol review. Further production work requires explicit operator direction addressing the failed backlog and resolver gates.

### 2026-08-25 — resolver JWKS remediation candidate verified; no production action

- **Production diagnosis:** the deployed candidate's resolver stopped at `sso_jwks` before scope policy. The separately authorized evidence had already shown refresh HTTP 200 and a scoped structure HTTP 200; CCP's current public JWKS mixes the RS256 RSA signing key with an ES256 EC key. The boundary decoder had incorrectly required RSA-only `n`/`e` fields on every JWKS entry.
- **Candidate:** `ff0960db75639157f67850477174c27c93a6f76e` (`fix(resolver): accept mixed SSO JWKS`), with no migration changes. It deserializes mixed JWKS entries, selects only a keyed RSA/RS256 entry with non-empty `n`/`e`, and preserves issuer, audience, expiry, subject, scope, and strict JWT `kid` validation.
- **TDD:** RED: the current mixed RSA/EC JWKS made no authenticated structure request; GREEN: one scoped loopback structure request persisted `structure_resolver_runtime=ready` and access-qualified evidence. A second RED showed a missing JWT/JWK `kid` could reach ESI; GREEN rejects it before ESI. Refresh-token rotation remains covered on the next-refresh wire without persistence or redaction leaks.
- **Authoritative review:** Sol high Standards/Spec review passed after the strict-`kid` remediation.
- **Verification:** one bounded serial real-PostgreSQL suite on a disposable loopback container passed `275/275` in `298.00s`; safe library suite passed `95`, with one documented credentialed visual test ignored. `cargo fmt --check`, `cargo check`, `cargo clippy -- -W clippy::all`, `git diff --check 3240ed03c217c3c5a0ff14c3621417190495bc1a..HEAD`, and quiet documented `docker compose config --quiet` passed. Clippy exited 0 with 87 pre-existing warnings only: 74 `uninlined_format_args`, 5 `too_many_arguments`, 3 `result_large_err`, 2 `type_complexity`, and one each of `large_enum_variant`, `needless_borrow`, and `needless_return`. The full suite includes clean/current migration coverage; the candidate changes no migration.
- **Boundary:** only the disposable test database was created and removed. No deployment, production database/authentication, Compose lifecycle, Discord, history, subscription, or historical-repair action occurred. Ticket 05 remains claimed pending approval.
