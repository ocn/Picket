# 04 — Deploy and verify the nano-activity feed

**What to build:** Deploy the verified named-role and configurable-cooldown behavior, then create a dedicated BIGAB Sonar Killmail Subscription that highlights high-value, non-NPC low/null-security kills by fewer-than-ten roaming attackers within 8 LY of Turnur or Kurniainen. Preserve the existing Fleet Composition Tally so members can identify active gangs and their composition.

**Blocked by:** none — Tickets 01–03 are deployed; the rollout completed with the recorded transient backlog concern.

**Status:** DONE_WITH_CONCERNS — one deployment completed; an initial deferred-backlog critical snapshot self-recovered over the next two watchdog evaluations without intervention.

**Deployment inputs:** Discord channel `1542022819808944228` (`🟧🛸-nano-activity`); target role `@Roamers` (`1542023064232005752`).

- [x] Confirm the `@Roamers` role exists.
- [x] Record the `@Roamers` numeric role ID and confirm it is mentionable or can be mentioned by the bot under Discord permissions.
- [x] Complete Release Verification and the applicable pre-mutation Rollout Health Gate before Deployment.
- [x] Confirm/adopt the existing target channel (`1542022819808944228`) and rename it to `🟧🛸-nano-activity` after every other pre-mutation gate passed.
- [x] Confirm the channel is named `🟧🛸-nano-activity`, is in the `🚔 8Ly of Turnur/Kurn 🚔` category, and has the same permission overwrites as `🟧👾💼-supers-sold`.
- [x] Create the `nano-activity` Killmail Subscription in the new channel.
- [x] Require a minimum total value of 5,000,000,000 ISK.
- [x] Restrict the subscription to systems within 8 LY of Turnur (`30002086`) or Kurniainen (`30003089`) and exclude highsec with security range `-1.0..=0.4`.
- [x] Exclude NPC-classified killmails.
- [x] Allow at most ten involved pilots under the existing filter semantics: no more than nine attacker records plus the victim.
- [x] Do not require zKillboard's solo flag or an attacker damage-share threshold.
- [x] Configure `@Roamers` with a 30-minute channel-wide ping cooldown and a 15-minute maximum killmail age.
- [x] Verify in focused candidate tests that every qualifying killmail posts when its role ping is stale or cooldown-suppressed.
- [x] Verify source-tested notification preparation preserves the existing Fleet Composition Tally, attacker count, affiliations, victim, Observed Location, and total-value presentation; no synthetic production post was sent.
- [x] Verify that `supers-sold` and existing capital-activity subscriptions are unchanged and retain their existing ping behavior.
- [x] Perform non-sending Production Verification without manufacturing a public combat claim or sending an unapproved synthetic `@Roamers` notification.
- [x] Record the deployed subscription, channel, role, configuration, and production evidence in the repository's established rollout artifacts.

## 2026-08-26 — pre-mutation evidence and controller clarification

- **Candidate boundary:** isolated candidate `2c3d05b` is exactly five commits ahead of merge-base `38425d6c9eee5001bc82aa89c29d27ae34a8f20f` with `main`: Ticket 01 `290a629`, Ticket 02 `cf6d78d`, and Ticket 03 `0087741`, `5f70748`, `2c3d05b`. Fixed-point check `git diff --check 38425d6c9eee5001bc82aa89c29d27ae34a8f20f..2c3d05b` exited 0 with no output. The reviewed candidate boundary is exactly five modified files: `README.md`, `src/commands/subscribe.rs`, `src/config.rs`, `src/discord_bot.rs`, and `src/pipeline.rs` (1,144 insertions, 132 deletions). No candidate image was built or deployed before the guarded deployment recorded below.
- **Release Verification:** `cargo fmt --check` and `cargo check` exited 0. Focused library tests passed: `cargo test --lib notification` 7/7, `cargo test --lib role` 6/6, and `cargo test --lib cooldown` 6/6. `cargo clippy -- -W clippy::all` exited 0 with the established 87 pre-existing warnings. The documented serial PostgreSQL suite on a disposable loopback-only PostgreSQL 16 container reported 292 passed and one failure after 399.91s: `regional_observation_batch_and_health_snapshot_migrations_apply_to_clean_and_current_databases` expected one `ClosedOutcomeUnknown` event and observed none. An exact rerun of only that test against a fresh disposable PostgreSQL 16 container passed 1/1 in 2.00s. The candidate does not modify that test, migrations, or contract code, so this was recorded as an aggregate-only isolated failure under the task rule; both temporary containers were removed.
- **Read-only Discord preflight:** API v10 showed channel `1542022819808944228` belongs to guild `888224317991706685`, has parent/category `915756482363023380` named `🚔 8Ly of Turnur/Kurn 🚔`, and has the same five normalized permission overwrites as reference `🟧👾💼-supers-sold` (`1117504110611157034`) in that category. The role named exactly `Roamers` is `1542023064232005752`; it is `mentionable: false`. Bot `1064713118707621980` has the guild `admin` role with Administrator (`8`), so its effective permissions include `MENTION_EVERYONE` (`131072`) and it can mention this otherwise-unmentionable role.
- **Controller clarification:** after the above preflight, the controller explicitly authorized this isolated name correction under the user's automatic-continuation authorization. The correction remains gated: complete every remaining release, production-health, backup, and Compose/config validation first; then rename only channel `1542022819808944228` to `🟧🛸-nano-activity`, re-query Discord evidence, and proceed only if that API mutation succeeds. Any failed remaining gate or rename API failure stops the rollout.
- **Current preservation state:** no permission overwrite update, channel update, backup, configuration change, Compose validation/deployment, restart, rollback, restore, production health gate, or non-sending production verification had occurred as of the initial preflight. The normalized killmail-subscription delta remains empty; `supers-sold`, capital-activity subscriptions, PostgreSQL, production images, containers, volumes, and live configuration remain untouched.

## 2026-08-26 — completed pre-mutation gates and one guarded deployment

- **Pre-mutation Rollout Health Gate:** initial and final snapshots had one bot (PID `58949`), one watchdog (PID `58938`), and healthy PostgreSQL (PID `683`), all restart `0` / `OOMKilled=false`. PostgreSQL retained container `killbot-contract-postgres`, volume `hazardous-killbot_contract-postgres`, 26/26 successful `_sqlx_migrations` rows, and all 26 stored SHA-384 checksums matching candidate migration files. There were zero ungranted locks/waiting connections and `contract_outbound_deliveries` had only `sent:1553`, so prepared/permanent delivery debt was zero. The current snapshot was degraded only by the recurring `structure resolver access denied` state, whose latest transition to degraded was `2026-08-25T09:41:11Z`; heartbeat, 114-region contract progress, ESI, backlog, prepared/permanent delivery, R2Z2, and feed validation were healthy. No fatal, Discord, database, panic, or config error matched in either service's preceding 30-minute logs. The live normalized guild subscription hash was `ebdaa8e81743cdeaca852799c2cb755c0910882b44ff709e3a1a1e21cc7aae78`; it contained no target record.
- **Recoverable backups and Compose validation:** at `20260826T062523Z`, JSON backup `backups/nano-activity-predeploy-20260826T062523Z.json` was mode `0600`, 238,842 bytes, SHA-256 `1b952b3ba77f7288b849c55c26c6a06ef194d1863944fb9fbdf5ec4540e148b8`, and passed `jq empty`. PostgreSQL custom backup `backups/nano-activity-predeploy-20260826T062523Z.dump` was mode `0600`, 88,005,066 bytes, SHA-256 `19d690bbf0e5502e7dbcf2160b571eb40e90694f17e9231ed99210d8e831ebd9`; container `pg_restore --list` completed with 183 entries. `docker compose --env-file .env -f docker-compose.yaml -f /tmp/hazardous-killbot-nano-activity-2c3d05b/compose.override.yaml config --quiet` exited 0.
- **Tracked candidate isolation and override provenance:** `git archive 2c3d05b` materialized `/tmp/hazardous-killbot-nano-activity-2c3d05b`, mode `0700`, with 180 tracked files and no `.git` or `.env`; its streamed archive SHA-256 was `8db807c5120e44538313a86276ec229790f624456fa723a95e5ead4bdd12df1f`. The deployment-local, sanitized override at `/tmp/hazardous-killbot-nano-activity-2c3d05b/compose.override.yaml` remained available for review: mode `0644`, 247 bytes, SHA-256 `362289c04077de6251b4db966da7e5879ef68f4d0b6e5f3d03e8c808ba8a76a1`, exact body:

  ```yaml
  services:
    discordbot:
      build:
        context: /tmp/hazardous-killbot-nano-activity-2c3d05b
        dockerfile: ./Dockerfile
    health-watchdog:
      build:
        context: /tmp/hazardous-killbot-nano-activity-2c3d05b
        dockerfile: ./Dockerfile
  ```

  The tracked Compose source gives those same two services `build.context: .` and `build.dockerfile: ./Dockerfile`; the override supplies only those two `build` fields for those two services. It adds no PostgreSQL service, image, environment, volume, command, health-check, network, or configuration change. Controller plan provenance requires building only exact tracked candidate `2c3d05b` while retaining the live-root Compose project, `.env`, `config`, services, and PostgreSQL volume as authoritative; the controller progress ledger fixes that candidate/base/worktree/branch. The one recorded deployment command names this exact override, so the reviewed body and hash prove that the archive-local input selected only the candidate build contexts. Dirty/untracked live-worktree files were excluded.
- **Prospective configuration and only-delta proof:** the prospective normalized hash was `24999fb6ddcd6972fe1a331e4e2fb6d9539423f1b180635d226817acc81182df`. Removing exactly `id=nano-activity`, `channel_id=1542022819808944228` from it reproduces the pre-mutation normalized JSON byte-for-byte. The candidate's actual `load_all_subscriptions` loader read the prospective file as 194 subscriptions and its Serde round trip matched exactly: `TotalValue min=5_000_000_000`, regions `[10000042,10000030,10000002,10000027,10000066]`, `IsNpc=false`, `Pilots max=10`, no `IsSolo` or damage-share condition, and Role `{ role_id=1542023064232005752, max_ping_delay_minutes=15, ping_cooldown_minutes=30 }`. Focused candidate tests also passed stale and cooldown suppression behavior without suppressing embeds.
- **Authorized Discord correction:** before correction, the target channel was `🟧🤏-nano`; its parent/category and five overwrite records already matched `🟧👾💼-supers-sold`. One `PATCH /channels/1542022819808944228` renamed only that channel to `🟧🛸-nano-activity`. Read-only re-query confirmed exact required name, parent `🚔 8Ly of Turnur/Kurn 🚔`, and overwrite parity. `Roamers` remained ID `1542023064232005752`, `mentionable=false`; the bot's Administrator permission remains sufficient for role mention authorization.
- **Atomic live configuration install:** immediately before replacement the live normalized hash still matched the backup baseline and the target was absent. A same-directory, mode-`0600` staging file passed JSON validation and was atomically renamed over `config/888224317991706685.json`. Its resulting normalized hash is the prospective `24999fb6ddcd6972fe1a331e4e2fb6d9539423f1b180635d226817acc81182df`; no other record changed.
- **Exactly one Compose deployment:** `docker compose --env-file .env -f docker-compose.yaml -f /tmp/hazardous-killbot-nano-activity-2c3d05b/compose.override.yaml up --build -d` completed exit 0. It built candidate image `sha256:3ddf3d006ba2537bd4445c96e1f7e172e51eec62952214d3fab3c8be51ef2ff9` and recreated exactly `discordbot` and `killbot-health-watchdog` at `2026-08-26T06:38:00Z`; PostgreSQL container, PID `683`, image, volume, restart count, and OOM state were unchanged. Each new application container had restart `0` / `OOMKilled=false`. Bot logs show `Loaded 194 subscriptions for guild 888224317991706685` and `Discord bot Killbot is connected!`.

## 2026-08-26 — post-deployment transient and non-sending Production Verification

- The first post-deployment watchdog snapshot at `2026-08-26T06:39:05.448375Z` was **critical**: `deferred_backlog` reported `10 due manifest or resolution backlog item(s); backlog grew from 3 to 10; oldest is 86790s old`. Heartbeat, 114-region progress, ESI, prepared/permanent delivery, R2Z2, and feed validation remained healthy; the only other non-healthy state was the pre-existing structure-resolver degradation.
- **Read-only recovery evidence:** no action was taken. `health_transitions` records recovery to healthy at `2026-08-26T06:40:05.754002Z` (4 due, oldest 29s). The two subsequent watchdog snapshots were healthy for deferred backlog: `06:41:05Z` (2 due, oldest 0s) and `06:42:06Z` (3 due, oldest 60s). Final `06:43:06Z` snapshot reported no due manifest/resolution backlog. Throughout, application containers remained the one expected recreated bot/watchdog pair (both restart `0`, `OOMKilled=false`); PostgreSQL remained PID `683`, delivery state remained only `sent:1553`, and no fatal/Discord/database/panic/config errors matched either service log since deployment.
- **Non-sending Production Verification:** final read-only Discord API result is exact channel `🟧🛸-nano-activity` (`1542022819808944228`) in `🚔 8Ly of Turnur/Kurn 🚔`, with overwrite parity to `🟧👾💼-supers-sold`; role `Roamers` remains `1542023064232005752`. The bot runs image `sha256:3ddf3d006ba2537bd4445c96e1f7e172e51eec62952214d3fab3c8be51ef2ff9`, mounts authoritative `/Users/sean/hazardous-killbot/config:/app/config`, and loaded 194 subscriptions. The live target JSON is exact and its normalized `24999fb6ddcd6972fe1a331e4e2fb6d9539423f1b180635d226817acc81182df` set differs from the validated pre-state only by `nano-activity`; existing capital-activity and reference-channel records are unchanged. No synthetic killmail, combat claim, or `@Roamers` notification was created or sent.
- **Concern and boundary:** the one-minute deferred-backlog critical state self-recovered, but is retained as a rollout concern. No second Compose operation, restart, retry, rollback, restore, configuration rewrite, or remediation was run. This tracker is the sole staged rollout artifact; backups, live config, temporary archive/validation files, `.superpowers`, and feature source are excluded from the commit.

## 2026-08-26 — proximity and security correction

- **Observed regression and cause:** production posted a six-year-old Jita kill because the deployed `nano-activity` record used the five broad region IDs and had no security condition. The 15-minute maximum age correctly controlled only the role mention, so it did not suppress the stale embed. The initial regression assertion required the intended two-center `LyRangeFrom` condition plus `Security = -1.0..=0.4` and failed against that deployed record.
- **Scoped correction:** backup `backups/nano-activity-proximity-fix-20260826T180529Z.json` is mode `0600`, 240,028 bytes, SHA-256 `2656fdf33e9a6ab606383cce718ad76e6fdbca56f7f5920a2cfebf117f4cadd4`, and passed `jq empty`. Only `nano-activity` changed: the region condition became `LyRangeFrom([{system_id:30002086,range:8.0},{system_id:30003089,range:8.0}])`, `Security = -1.0..=0.4` was added, and the description was corrected. Its value, NPC, pilot, role, 15-minute ping-age, and 30-minute cooldown conditions are unchanged; removing `nano-activity` from the before/after documents produces byte-identical normalized JSON.
- **Verification and reload:** the regression assertion passes after the correction, `jq empty` passes, and the live file remains mode `0600` with 194 subscriptions. Focused `test_ly_filter` and `test_security_filter` tests each passed 1/1. One configuration-only `docker restart discordbot` kept image `sha256:3ddf3d006ba2537bd4445c96e1f7e172e51eec62952214d3fab3c8be51ef2ff9`; startup loaded 194 guild subscriptions and reconnected to Discord. The resulting normalized configuration SHA-256 is `2c4b6c4ec6fc01801ea3bc4fec8a3703032819cf100f7a800e8bd7777d028c41`.
- **Post-restart health:** container state is running with restart count `0` and `OOMKilled=false`. The `2026-08-26T18:06:32.450653Z` watchdog snapshot is degraded only by the pre-existing structure-resolver access denial; heartbeat, 114-region contract progress, ESI progress, deferred backlog, prepared/permanent delivery, R2Z2 progress, and feed validation are healthy. No synthetic message or ping was sent.
